---
title: "Local Model Smoke Test Prompt Set"
artifact_type: "benchmark_prompt_set"
created_date: "2026-05-12"
status: "completed"
---

# Local Model Smoke Test Prompt Set

## Test 1: exact instruction following

Reply with exactly this sentence and nothing else:

Local model instruction following is working.

## Test 2: voice transcript cleanup

Clean up this dictated text without changing the meaning:

"I wanna use the Mac Studio as the local work bench and not let it become another swamp of random AI junk, but I also want to break it now and fix it now instead of coming back later."

## Test 3: YAML generation

Create YAML front matter for a Markdown file documenting a local AI benchmark. Include title, artifact_type, created_date, model, runtime, status, and tags.

## Test 4: concise architecture summary

In five bullets, summarize this architecture:

Notion is the working canon. GitHub is the durable corpus. Mac Studio is the local workbench. Ollama and LM Studio run local models. Hugging Face supplies model artifacts. Replit is the expensive build executor. ChatGPT and Claude are the senior reasoning layer.

## Test 5: Mermaid generation

Create a Mermaid flowchart showing:

ChatGPT and Claude feed Notion. Notion feeds GitHub. GitHub feeds Mac Studio. Mac Studio runs Ollama, LM Studio, and Hugging Face cache. Replit builds from GitHub and sends changes back to GitHub.
